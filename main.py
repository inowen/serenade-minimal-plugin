# create the state dictionary

# create the gui window and run its update loop asynchronously

# create the command handler and start listening to commands
# (pass state to the command handler so the callbacks can change it)
